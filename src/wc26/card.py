"""Phase 3: Generate a static PNG match-recap card for LinkedIn.

Placeholder implementation — will be fully fleshed out in Phase 3.
"""

from __future__ import annotations

import json
import logging
from pathlib import Path

logger = logging.getLogger(__name__)


def generate_linkedin_card(outputs_dir: Path, out_path: Path | None = None) -> Path | None:
    """Generate a shareable PNG summary card.

    Phase 3 placeholder — returns None until implemented.
    """
    logger.info("LinkedIn card generation: Phase 3 placeholder (skipped)")
    return None
